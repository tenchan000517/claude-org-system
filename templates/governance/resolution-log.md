<!-- The org's memory of "what we've already settled." Append-only in spirit. Once recorded, a
     resolution binds roles until it is explicitly re-decided. For a solo operator this can live inside
     the secretary as the decisions list. -->

# Resolution log

| # | Date | Resolution | Why | Applies to |
|---|---|---|---|---|
| 1 | YYYY-MM-DD | <the direction-level decision> | <the reasoning that makes it durable> | <which roles/departments must comply> |
| 2 | YYYY-MM-DD | … | … | … |

## Notes
- This logs **direction-level** decisions, not day-to-day choices.
- A recorded resolution is followed, not relitigated each session. To change one, raise it to be
  re-decided and add a new row that supersedes the old (note which # it replaces).
