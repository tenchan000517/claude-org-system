<!-- A department's base working document, derived from the central CREDO. Place at the root of the
     department's directory as CLAUDE.md. This is the "each department's own credo" — it states the
     department's role and targets, and INHERITS the org's conduct principles rather than restating them. -->

# <Department> — working document

This department's base credo. It derives from the org `CREDO.md`; read that first. This document does
not restate the org conduct principles — it inherits them.

## 1. Role in the org
- **Domain:** <the coherent area this department owns>
- **Center type:** <profit/impact | cost/enabling> — <one line on how it relates to the org's headline goal>
- **In scope:** <what this department does>
- **Out of scope:** <what it explicitly does NOT do — cross-domain ideas go up as proposals, not self-started>

## 2. Targets (what "winning" means here)
- **KGI (or definition of done):** <the outcome this department is accountable for>
- **KPI:** <the leading indicators it manages>
- **Cadence / deadline:** <how progress is paced>

## 3. How to read on startup
- **Required:** this document + this department's `HANDOFF.md` (current position)
- **As needed:** org `CREDO.md`, judgment axis (`framework/06-judgment.md`), and the **secretary**
  (`secretary/KB.md`) for infrastructure/lookups

## 4. Conduct
- Inherits all org conduct principles from `CREDO.md` (verify-don't-assume, minimal interpretation,
  requirements alignment, cost discipline, safety=reversibility, protect the assets, confidentiality,
  report faithfully). Add only **department-specific** rules here, if any:
  - <e.g. a quality bar this department must pass before shipping>
  - <e.g. a domain-specific confidentiality nuance>

## 5. Lead's responsibilities (if this department has a lead)
- Owns this domain's outcome; translates org goals into the KPIs above.
- Proposes the pivot or the stop if the domain is missing its target (failure/withdrawal judgment).
- Reports up in the fixed format (`templates/reports/monthly-report.md`) on the org cadence.
